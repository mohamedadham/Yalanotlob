<div class="media-heading">
  <% if notification.second_target != nil %>
  <%= link_to notification.actor.name, main_app.user_path(notification.actor)  %> 
  <%=
  if notification.second_target.status === "accepted"
    "joined your "
  else
    "rejected your"
    end
  %>
  <%= link_to "order", main_app.order_path(notification.user) %> 
</div>
 <% end %>